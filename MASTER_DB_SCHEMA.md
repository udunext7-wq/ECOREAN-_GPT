# ECOREAN Master DB 스키마

## 1. Master DB 정의

Master DB는 ECOREAN 자동견적 OS의 기준 데이터베이스다.

단순 단가표가 아니라 다음 정보를 포함한다.

- 공정 트리
- 기본 사양
- 옵션 그룹
- 가격 계산 로직
- 수량 산출 로직
- 공정표 로직
- 온톨로지 관계
- 출력 정책
- 버전 관리

## 2. ProcessItem

Master DB의 핵심 단위는 `ProcessItem`이다.

```ts
type ProcessItem = {
  code: string;
  name: string;
  hierarchy: ProcessHierarchy;
  defaultSpec: DefaultSpec;
  optionGroups: OptionGroup[];
  ontologyRelation: OntologyRelation;
  triggerType: TriggerType;
  quantityLogic: QuantityLogic;
  priceLogic: PriceLogic;
  scheduleLogic: ScheduleLogic;
  outputPolicy: OutputPolicy;
  governance: Governance;
};
```

## 18. Conditional Process Generation Policy

공정은 공간명만 보고 자동 생성하지 않는다. 욕실이라는 공간이 있다고 해서 방수, 철거, 배관 수정, 타일 재시공이 무조건 포함되면 안 된다.

공정 생성 여부는 현장 조건, 철거 범위, 기존 상태, 고객 선택, 선행공정, 하자 리스크, 법규/안전 조건, Case Library 피드백으로 판단한다.

모든 공정 DB 항목은 다음 필드를 포함하거나 상속해야 한다.

```ts
type ProcessTriggerPolicy = {
  triggerType: 'AUTO' | 'SELECT' | 'QTY' | 'CONDITIONAL' | 'NEEDS_CONFIRMATION';
  triggerConditions: string[];
  exclusionConditions: string[];
  needsConfirmationWhen: string[];
  autoGeneratedReason: string;
  customerExplanation: string;
  internalReasonLog: string[];
  relatedRiskFlags: string[];
  diagnosticWarnings: string[];
  caseLibraryFeedback: {
    recordTriggerDecision: boolean;
    recordExclusionDecision: boolean;
    recordActualOutcome: boolean;
  };
};
```

AUTO는 정말 항상 필요한 항목에만 제한한다. 조건이 부족한 공정은 자동 포함하지 않고 `NEEDS_CONFIRMATION` 상태로 둔다.

다음 공정군은 반드시 CONDITIONAL 우선으로 관리한다.

- 방수
- 배관 수정
- 전기 증설
- 철거
- 폐기물
- 보강
- 몰딩 교체
- 문틀 교체
- 창호 교체
- 타일 재시공
- 바닥 철거
- 단열
- 결로 보수
- 누수 보수
- 하자 보수
- 추가공사

공정 삭제 시 연결된 견적, 공정표, 발주표, 인력 배정, 현금흐름, 검수표, 리스크 노드도 함께 제거 또는 무효화되어야 한다.

## 3. hierarchy

```ts
type ProcessHierarchy = {
  majorCategory: string;
  middleCategory: string;
  minorCategory: string;
  detailCategory: string;
};
```

예:

```text
습식공정 -> 타일 -> 욕실 바닥 타일 -> 포세린 타일
```

## 4. defaultSpec

```ts
type DefaultSpec = {
  name: string;
  description?: string;
  unit: 'm2' | 'm' | 'ea' | 'set' | 'day' | 'ton' | 'lot';
  standardMaterial?: string;
  standardMethod?: string;
  standardThickness?: string;
  standardGrade?: string;
  standardWasteRate?: number;
  standardProductivity?: number;
};
```

원칙:

- 선택하지 않아도 견적 계산이 가능해야 한다.
- 가장 많이 쓰는 표준 사양을 기본값으로 둔다.
- 변경 시 Master DB 버전이 바뀐다.

## 5. optionGroups

```ts
type OptionGroup = {
  key: string;
  name: string;
  required: boolean;
  selectionType: 'single' | 'multiple';
  defaultOptionKey?: string;
  options: ProcessOption[];
};

type ProcessOption = {
  key: string;
  name: string;
  priceAdjustType: 'amount' | 'rate' | 'multiplier';
  laborAdjust?: number;
  materialAdjust?: number;
  equipmentAdjust?: number;
  durationAdjust?: number;
  difficultyAdjust?: number;
  description?: string;
};
```

옵션 그룹 예:

- 프레임
- 두께
- 자재등급
- 시공방식
- 난이도
- 철거 여부
- 마감 등급

## 6. ontologyRelation

```ts
type OntologyRelation = {
  spaces: string[];
  materials: string[];
  laborRoles: string[];
  equipment: string[];
  prerequisiteProcesses: string[];
  nextProcesses: string[];
  requiredConditions: string[];
  riskFactors: string[];
  relatedOutputs: string[];
};
```

목적:

- 누락 공정 탐지
- 선행/후행 공정 계산
- 자재 발주표 생성
- 인력 투입표 생성
- 현장 리스크 표시
- AI 판단 근거 제공

## 7. triggerType

```ts
type TriggerType = 'AUTO' | 'SELECT' | 'QTY' | 'CONDITIONAL';
```

정의:

- `AUTO`: 조건 충족 시 자동 포함
- `SELECT`: 사용자가 선택해야 포함
- `QTY`: 수량 입력 시 포함
- `CONDITIONAL`: 조건 조합에 따라 포함

## 8. quantityLogic

```ts
type QuantityLogic = {
  sourceType:
    | 'manual'
    | 'spaceArea'
    | 'wallArea'
    | 'floorArea'
    | 'ceilingArea'
    | 'openingArea'
    | 'count'
    | 'formula';
  formula?: string;
  wasteRate?: number;
  minQuantity?: number;
  roundingRule?: 'none' | 'ceil' | 'floor' | 'round';
};
```

## 9. priceLogic

```ts
type PriceLogic = {
  basePrice: number;
  laborCost: number;
  materialCost: number;
  equipmentCost: number;
  optionAdjust: PriceAdjustmentRule[];
  difficultyAdjust: PriceAdjustmentRule[];
  regionAdjust?: PriceAdjustmentRule[];
  finalPriceFormula: string;
};

type PriceAdjustmentRule = {
  key: string;
  name: string;
  adjustType: 'amount' | 'rate' | 'multiplier';
  target:
    | 'basePrice'
    | 'laborCost'
    | 'materialCost'
    | 'equipmentCost'
    | 'finalPrice';
  value: number;
};
```

기본 계산 개념:

```text
baseCost = laborCost + materialCost + equipmentCost
adjustedCost = baseCost + optionAdjust + difficultyAdjust + regionAdjust
finalPrice = adjustedCost * marginMultiplier
```

## 10. scheduleLogic

```ts
type ScheduleLogic = {
  defaultDuration: number;
  productivityUnitPerDay?: number;
  minDuration?: number;
  maxCrewPerDay?: number;
  requiredCrew: LaborRequirement[];
  dependencies: string[];
  canOverlapWith: string[];
  cannotOverlapWith: string[];
  curingTimeDays?: number;
};

type LaborRequirement = {
  role: string;
  count: number;
};
```

## 11. outputPolicy

```ts
type OutputPolicy = {
  showInCustomerEstimate: boolean;
  customerDisplayName?: string;
  customerGroupName?: string;
  showInInternalCostSheet: boolean;
  showInMaterialOrderSheet: boolean;
  showInLaborPlan: boolean;
  showInSchedule: boolean;
  showRiskInternally: boolean;
};
```

## 12. governance

```ts
type Governance = {
  version: string;
  status: 'draft' | 'active' | 'deprecated';
  effectiveFrom: string;
  effectiveTo?: string;
  createdBy: string;
  approvedBy?: string;
  changeReason?: string;
};
```

## 13. 변경 승인 원칙

다음 변경은 대표님 승인 후 진행한다.

- ProcessItem 필드 추가/삭제
- triggerType 정의 변경
- priceLogic 계산 구조 변경
- defaultSpec 변경
- optionGroups 변경
- ontologyRelation 변경
- 단가 변경
- 마진율 변경
- 표준 사양 변경

## 14. v0.2 제안: 자재/부자재 영향 구조

모든 공정 DB에는 주자재뿐 아니라 다음 항목을 포함하는 구조가 필요하다.

```text
부자재
소모품
접착재
보양재
운반비
양중비
폐기물
손실률
시공 난이도
```

세부 스키마는 `material-impact-schema.md`를 기준으로 한다.

주의:

```text
이 항목은 스키마 v0.2 제안안이다.
실제 Master DB 스키마 반영 전 대표님 승인이 필요하다.
```

핵심 추가 구조:

```ts
type ProcessItemV02 = {
  code: string;
  name: string;
  hierarchy: ProcessHierarchy;
  defaultSpec: DefaultSpec;
  materialProfile: ProcessMaterialProfile;
  optionGroups: OptionGroupV02[];
  ontologyRelation: OntologyRelation;
  triggerType: TriggerType;
  quantityLogic: QuantityLogic;
  priceLogic: PriceLogic;
  scheduleLogic: ScheduleLogic;
  outputPolicy: OutputPolicy;
  governance: Governance;
};
```

## 15. v0.3 제안: 단가 출처 및 가격 기준 구조

ECOREAN Master DB는 공식 단가와 시장 단가, 공급가, 내부 실적가를 분리해야 한다.

단가 출처는 다음 4단계로 구분한다.

```text
official
supplier
market
internal
```

모든 단가 데이터는 다음 필드를 가진다.

```ts
type PriceRecord = {
  priceId: string;
  itemId: string;
  itemName: string;
  unit: string;
  basePrice: number;
  laborCost: number;
  materialCost: number;
  equipmentCost: number;
  accessoryCost: number;
  wasteRate: number;
  sourceType: 'official' | 'supplier' | 'market' | 'internal';
  sourceName: string;
  sourceDate: string;
  confidenceLevel: 'low' | 'medium' | 'high' | 'verified';
  priceStatus: 'draft' | 'active' | 'outdated' | 'deprecated' | 'needsReview';
  updateCycle:
    | 'weekly'
    | 'monthly'
    | 'quarterly'
    | 'semiAnnual'
    | 'annual'
    | 'perProject'
    | 'onChange';
  notes?: string;
};
```

최종 견적에는 어떤 가격 기준을 사용했는지 반드시 기록한다.

```ts
type PriceBasis =
  | 'officialPrice'
  | 'supplierPrice'
  | 'marketPrice'
  | 'internalPrice'
  | 'manualOverride';
```

상세 기준은 `price-source-policy.md`를 따른다.

주의:

```text
이 항목은 Master DB 스키마 변경에 해당하므로 실제 반영 전 대표님 승인 필요.
```

## 16. 공정 필수 구조 요약

모든 공정은 추가로 반드시 아래 구조를 가진다.

```text
defaultSpec
optionGroups
ontologyRelation
triggerType
dependencies
appliesToSpaces
spaceFactor
quantityFormula
minimumLaborCharge
batchingRule
mobilizationCost
crewType
crewProductivity
laborAggregationRule
defaultDuration
leadTimeDays
customerVisible
internalOnly
```

공정 포함 원칙:

```text
선택되지 않은 공정은 견적에 포함하지 않는다.
선택된 공정은 defaultSpec 기준으로 계산한다.
옵션 변경 시 priceAdjust, laborImpact, materialImpact, durationImpact를 반영해 즉시 재계산한다.
```

## 17. v0.4 제안: 공정 중심 통합 계산 구조

인테리어 견적은 공간별 단독 계산이 아니라 공정별 통합 계산 + 공간별 적용 조건 방식으로 설계한다.

예를 들어 타일공정은 욕실, 주방, 현관, 발코니, 다용도실 등에 모두 적용된다.

따라서 욕실 타일, 주방 타일, 현관 타일을 각각 별도 인건비로 계산하면 실제 시공 구조와 다르게 왜곡된다.

정확한 구조:

```text
공간별 입력
-> 공정별 통합
-> 공간별 난이도 보정
-> 최소 품수 적용
-> 통합 인건비 산출
-> 공간별 고객 견적 분배
```

### 17.1 설계 원칙

```text
1. 공정 DB는 공정 중심으로 관리한다.
2. 공간은 공정의 적용 조건으로 관리한다.
3. 같은 공정이 여러 공간에 적용되면 인건비는 통합 계산한다.
4. 최소 인건비는 공정별로 적용한다.
5. 소량 공정은 minimumLaborCharge를 적용한다.
6. 여러 공간의 동일 공정은 batchingRule에 따라 묶어 계산한다.
7. 공간별 난이도는 spaceFactor로 보정한다.
8. 자재와 부자재는 공간별 산출 후 합산한다.
9. 인건비는 공정별 총량 기준으로 계산한다.
10. 고객용 견적서에는 공간별로 보기 쉽게 표시하되, 내부 원가표에는 공정별 통합 인건비가 보이게 한다.
```

### 17.2 추가 필드

```ts
type ProcessAggregationPolicy = {
  appliesToSpaces: AppliesToSpace[];
  spaceFactor: SpaceFactorRule[];
  minimumLaborCharge: MinimumLaborCharge;
  batchingRule: BatchingRule;
  mobilizationCost: MobilizationCost;
  crewType: CrewType;
  crewProductivity: CrewProductivity;
  laborAggregationRule: LaborAggregationRule;
};
```

### 17.3 appliesToSpaces

공정이 적용 가능한 공간이다.

```ts
type AppliesToSpace = {
  spaceType: string;
  defaultIncluded: boolean;
  quantitySource: string;
  customerDisplayGroup?: string;
  internalCostGroup?: string;
};
```

예:

```text
욕실
주방
현관
발코니
다용도실
```

### 17.4 spaceFactor

공간별 난이도 보정값이다.

```ts
type SpaceFactorRule = {
  spaceType: string;
  laborMultiplier: number | 'NEEDS_RESEARCH';
  materialMultiplier: number | 'NEEDS_RESEARCH';
  accessoryMultiplier: number | 'NEEDS_RESEARCH';
  durationMultiplier: number | 'NEEDS_RESEARCH';
  reason?: string;
};
```

예:

```text
욕실: 방수, 구배, 설비 간섭 때문에 난이도 높음
주방 벽: 면적은 작지만 컷팅과 콘센트 간섭 있음
현관 바닥: 소량이므로 최소 품수 영향 큼
발코니 바닥: 구배와 배수 조건 영향 있음
```

### 17.5 minimumLaborCharge

소량 공정의 최소 품수 또는 최소 인건비 기준이다.

```ts
type MinimumLaborCharge = {
  enabled: boolean;
  minimumCrewDays: number | 'NEEDS_RESEARCH';
  minimumLaborCost: number | 'NEEDS_RESEARCH';
  applyWhenQuantityBelow?: number | 'NEEDS_RESEARCH';
  note?: string;
};
```

### 17.6 batchingRule

여러 공간의 동일 공정을 묶어 계산하는 규칙이다.

```ts
type BatchingRule = {
  batchBy: 'processCode' | 'crewType' | 'materialType' | 'siteVisit';
  allowCrossSpaceBatching: boolean;
  batchWindowDays: number | 'NEEDS_RESEARCH';
  separateBatchConditions?: string[];
  note?: string;
};
```

### 17.7 mobilizationCost

시공팀 1회 투입 비용이다.

```ts
type MobilizationCost = {
  enabled: boolean;
  cost: number | 'NEEDS_RESEARCH';
  appliesPer: 'project' | 'process' | 'crewVisit';
  note?: string;
};
```

### 17.8 crewType

공정을 수행하는 팀 유형이다.

```ts
type CrewType = {
  code: string;
  name: string;
  roles: LaborRequirement[];
};
```

예:

```text
TILE_CREW_STANDARD = 타일공 1명 + 조공 1명
```

### 17.9 crewProductivity

시공팀의 생산성 기준이다.

```ts
type CrewProductivity = {
  unit: string;
  quantityPerCrewDay: number | 'NEEDS_RESEARCH';
  productivityBasis: 'totalQuantity' | 'weightedQuantity';
  note?: string;
};
```

### 17.10 laborAggregationRule

인건비를 어떤 기준으로 통합 계산할지 정의한다.

```ts
type LaborAggregationRule = {
  aggregateBy: 'processCode' | 'crewType' | 'siteVisit';
  quantityBasis: 'rawQuantity' | 'spaceFactorWeightedQuantity';
  applyMinimumLaborCharge: boolean;
  distributeToCustomerLines: boolean;
  customerDistributionBasis: 'spaceQuantity' | 'weightedQuantity' | 'manualRatio';
  internalDisplayBasis: 'aggregatedProcessLabor' | 'spaceSplitLabor';
};
```

### 17.11 타일 공정 예시

입력:

```text
욕실 타일 15㎡
주방 벽타일 5㎡
현관 바닥타일 3㎡
발코니 바닥타일 8㎡
```

공정 중심 계산:

```text
공정: 타일
총 면적: 31㎡
팀: 타일공 1팀
자재/부자재: 공간별 산출 후 합산
인건비: 총 타일 면적 + 공간별 난이도 보정 후 통합 계산
최소 품수: 타일공정 전체 기준으로 1회 적용
폐기/운반: 타일공정 전체 기준으로 통합 적용
```

출력:

```text
고객용 견적서:
- 욕실 타일
- 주방 벽타일
- 현관 바닥타일
- 발코니 바닥타일

내부 원가표:
- 타일공정 통합 인건비
- 타일 주자재 합계
- 접착재/줄눈재/소모품 합계
- 운반/양중 합계
- 폐기물 합계
```
