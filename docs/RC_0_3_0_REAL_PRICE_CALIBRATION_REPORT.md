# RC-0.3.0 실제 단가 보정 리포트

이 파일은 `실제 단가 보정` 화면의 `리포트 생성` 기능으로 갱신될 수 있다.

## 초기 상태

- 초기 기준 데이터 단가는 `NEEDS_UPDATE` 상태다.
- 실제 업체 견적, 매입가, 노무 단가 입력 후 승인/백업/반영을 거쳐야 다음 견적 기준으로 사용할 수 있다.

## 주의

이 리포트의 가격은 사용자가 입력한 견적/매입/노무 단가를 기준으로 한다.
외부 시장 단가로 자동 검증된 값이 아니다.
